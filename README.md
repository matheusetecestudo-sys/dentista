# Clínica OdontoPremium - Landing Page

Esta é uma landing page moderna para uma clínica odontológica de alto padrão, desenvolvida com React, TypeScript, Tailwind CSS e Framer Motion.

## 🚀 Tecnologias Utilizadas

- **React** + **Vite**: Para performance e desenvolvimento rápido.
- **TypeScript**: Para tipagem segura e código robusto.
- **Tailwind CSS**: Para estilização moderna e responsiva.
- **Framer Motion**: Para animações fluidas e interações avançadas.
- **Lucide React**: Para ícones leves e elegantes.

## 🛠️ Como rodar o projeto localmente

1. Clone o repositório.
2. Instale as dependências:
   ```bash
   npm install
   ```
3. Rode o servidor de desenvolvimento:
   ```bash
   npm run dev
   ```

## 📦 Deploy na Vercel

O projeto já está configurado para deploy na Vercel.

1. Crie uma conta na [Vercel](https://vercel.com/).
2. Importe o repositório do GitHub.
3. A Vercel detectará automaticamente que é um projeto Vite e preencherá as configurações de build.
4. Clique em **Deploy**.

O arquivo `vercel.json` incluído na raiz garante a configuração correta de rotas e cache.

## 🎨 Personalização

- As cores e fontes podem ser alteradas no arquivo `tailwind.config.js`.
- O conteúdo textual está distribuído nos componentes em `src/components`.
- Imagens são carregadas via URL (Unsplash), mas podem ser substituídas por assets locais na pasta `public` ou `src/assets`.
